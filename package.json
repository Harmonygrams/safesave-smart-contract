{
  "name": "SaveFunds",
  "version": "1.0.0",
  "description": "A smart contract that runs on ethereum blockchain for savings. ",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@chainlink/contracts": "^0.6.1",
    "dotenv": "^16.0.3",
    "ethers": "^6.3.0",
    "solc": "0.8.8"
  },
  "scripts": {
    "compile": "solcjs --bin --abi --include-path node_modules/ --base-path . SafeSave.sol"
  }
}
